### 简单说明

#### 内部类 inner
- 用 Outer.Nested() 初始化
- 匿名内部类
  - 暂时略


#### 枚举类 enum
- 常见 enum Direction
- 带参数的 enum Color
- 匿名枚举类 ProtocolState (IntArithmetics)
  - enum 中有个 抽象方法 
    - 枚举对象，需要实现这个方法
    - 匿名类实现的
  - enum 的遍历
    - for (f in IntArithmetics.entries)

#### 内联类 Inline value classes





