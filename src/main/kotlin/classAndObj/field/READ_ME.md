### 简单说明

#### Address
- 对应的属性
- 方法，copy对象


#### Rectangle
- 定义 area 的 get() 方法， 直接用 对象.area 调用 get()方法
- 在 属性下面添加 private set
  - 外部不能 set
  - 内部可以 set
- 在 set 中判断后 （幕后字段）
  - 可以用 field = value 处理当前 属性
- 
